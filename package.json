{
  "name": "project",
  "version": "1.0.0",
  "scripts": {
    "frontend": "cd frontend && npm run dev", 
    "backend": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0" 
  }
}